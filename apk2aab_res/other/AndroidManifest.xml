<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="${applicationId}"
    platformBuildVersionCode="${platformBuildVersionCode}"
    platformBuildVersionName="${platformBuildVersionName}"
    split="${moduleName}"
    android:compileSdkVersion="${compileSdkVersion}"
    android:compileSdkVersionCodename="${compileSdkVersionCodename}" >
 
    <dist:module dist:type="asset-pack" >
        <dist:fusing dist:include="true" />
 
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
    </dist:module>
 
</manifest>